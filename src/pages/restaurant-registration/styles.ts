import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    padding: 16,
    backgroundColor: '#fff',
    flexGrow: 1,
  },
  sectionTitle: {
    fontWeight: 'bold',
    marginTop: 20,
    marginBottom: 4,
  },
  ufLabel: {
    marginTop: 8,
    marginBottom: 4,
    fontWeight: 'bold',
  },
  pickerContainer: {
    borderWidth: 1,
    borderColor: '#bbb',
    borderRadius: 4,
    marginBottom: 16,
  },
  picker: {
    height: 50,
    width: '100%',
  },
  maskedInput: {
    borderWidth: 1,
    borderColor: '#bbb',
    borderRadius: 4,
    padding: 12,
    fontSize: 16,
    marginBottom: 4,
  },
  button: {
    marginTop: 24,
    marginBottom: 40
  },
});